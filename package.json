{
  "name": "lukekarrys.com",
  "description": "My personal site.",
  "version": "1.0.0",
  "dependencies": {
    "colors.css": "^2.3.0",
    "dom-events": "^0.1.1",
    "geopattern": "^1.2.3",
    "waypoints": "^3.1.1"
  },
  "devDependencies": {
    "babelify": "^6.1.3",
    "connect-livereload": "^0.5.3",
    "git-directory-deploy": "^1.1.0",
    "grunt": "^0.4.5",
    "grunt-browserify": "^3.8.0",
    "grunt-cli": "^0.1.13",
    "grunt-contrib-connect": "^0.10.1",
    "grunt-contrib-copy": "^0.8.0",
    "grunt-contrib-cssmin": "^0.12.3",
    "grunt-contrib-jade": "^0.15.0",
    "grunt-contrib-less": "^1.0.1",
    "grunt-contrib-uglify": "^0.9.1",
    "grunt-contrib-watch": "^0.6.1",
    "less-plugin-autoprefix": "^1.4.2",
    "standard": "^5.0.0-2"
  },
  "engines": {
    "node": "^0.10.40",
    "npm": "^2.14.0"
  },
  "license": "ISC",
  "main": "Gruntfile.js",
  "private": true,
  "scripts": {
    "build": "NODE_ENV=production grunt build",
    "codeship-setup": "npm run npm-version && npm install && npm run build",
    "codeship-test": "npm run lint && npm run validate",
    "deploy": "git-directory-deploy --directory public --username codeship --email lukekarrys@gmail.com",
    "lint": "standard",
    "npm-version": "npm install -g npm@`cat package.json | jq -r '.engines.npm'`",
    "start": "NODE_ENV=development grunt serve",
    "validate": "npm ls"
  },
  "standard": {
    "ignore": [
      "public",
      "_assets"
    ]
  }
}
